# cat_help; display help content from a file
# cat_help [help_file]

cat_help() {
  if [[ -f "$1" ]]; then
    cat "$1"
  else
    stderr "Error: Help file not found. Please check script installation."
    return 1
  fi
}
